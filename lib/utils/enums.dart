enum PlayerConnectivityStatus{
  // Player Connected to the server
  connected,

  // Player reconnected to the server
  reconnected,

  // Player reconnecting to the server
  reconnecting,

  // Player disconnected or kicked from the server
  disconnected,
}